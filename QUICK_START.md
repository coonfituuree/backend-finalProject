# 🚀 QUICK START GUIDE - Быстрый старт проекта

## ✅ Требования

- Node.js >= 18.0.0
- MongoDB >= 5.0.0
- npm или pnpm

---

## 📦 ШАГ 1: Установка Backend

```bash
# Перейти в папку backend
cd backend

# Установить зависимости
npm install
# или
pnpm install

# Создать файл .env
```

### Содержимое файла `backend/.env`:

```
PORT=4000
MONGODB_URI=mongodb://localhost:27017/vizier_airways
JWT_SECRET=your_super_secret_jwt_key_2025
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_email_password
FRONTEND_URL=http://localhost:9999
```

### Запуск Backend

```bash
npm run dev
```

✅ Backend будет доступен на `http://localhost:4000`

---

## 📦 ШАГ 2: Установка Frontend

**В новом терминале:**

```bash
# Перейти в папку frontend
cd frontend

# Установить зависимости
npm install
# или
pnpm install

# Создать файл .env.local
```

### Содержимое файла `frontend/.env.local`:

```
NEXT_PUBLIC_API_URL=http://localhost:4000/api
```

### Запуск Frontend

```bash
npm run dev
```

✅ Frontend будет доступен на `http://localhost:9999`

---

## 🎯 ШАГ 3: Использование приложения

### 1️⃣ Открыть приложение

- Перейдите на http://localhost:9999

### 2️⃣ Создать аккаунт

- Нажмите "Вход"
- Переключитесь на "Регистрация"
- Заполните форму:
  - Имя: Иван
  - Фамилия: Иванов
  - Имя пользователя: ivan_94
  - Email: ivan@test.com
  - Номер телефона: +7 (999) 999-99-99
  - Пароль: Test123456

### 3️⃣ Поиск полетов

- На главной странице найдите форму поиска
- Введите:
  - **Откуда:** Москва
  - **Куда:** Казань
- Нажмите кнопку поиска

### 4️⃣ Бронирование

- Нажмите "Забронировать" на выбранном рейсе
- Заполните данные пассажиров
- Выберите класс: Эконом или Бизнес
- Нажмите "Подтвердить и оплатить"

### 5️⃣ Оплата

- Используйте **тестовую карту**: `4111 1111 1111 1111`
- Месяц: любой (01-12)
- Год: любой будущий (25-99)
- CVV: 123
- Нажмите "Оплатить"

### 6️⃣ Управление бронированиями

- Перейдите на `/bookings`
- Просмотрите все ваши бронирования
- Отмените бронирование при необходимости

### 7️⃣ Профиль

- Нажмите на "Профиль" в навигации
- Отредактируйте свои данные
- Посмотрите информацию об аккаунте

---

## 🔑 Админ-функции

Если ваш аккаунт имеет роль администратора:

### Управление пользователями

1. Перейдите на `/admin`
2. Откройте вкладку "👥 Пользователи"
3. Просмотрите всех зарегистрированных пользователей

### Добавить один рейс

1. На `/admin` откройте вкладку "➕ Добавить рейс"
2. Заполните форму с деталями рейса
3. Нажмите "Добавить рейс"

### Массовый импорт рейсов (JSON)

1. На `/admin` в разделе "➕ Добавить рейс"
2. Переключитесь на "📦 Добавить несколько рейсов (JSON)"
3. Вставьте JSON с рейсами
4. Нажмите "Добавить рейсы"

**Пример JSON для импорта:**

```json
[
  {
    "from": "Москва",
    "fromAirport": "SVO",
    "to": "Казань",
    "toAirport": "KZN",
    "operatedBy": "Аэрофлот",
    "flightNumber": "SU100",
    "airplaneType": "Boeing 737",
    "departureTime": "2025-02-15T10:00:00",
    "arrivalTime": "2025-02-15T12:00:00",
    "flightDuration": "2h 0min",
    "numberOfTransfers": "0",
    "EconomPrice": 4500,
    "businessPrice": 9000
  },
  {
    "from": "Казань",
    "fromAirport": "KZN",
    "to": "Москва",
    "toAirport": "SVO",
    "operatedBy": "Аэрофлот",
    "flightNumber": "SU101",
    "airplaneType": "Airbus A320",
    "departureTime": "2025-02-15T14:00:00",
    "arrivalTime": "2025-02-15T16:00:00",
    "flightDuration": "2h 0min",
    "numberOfTransfers": "0",
    "EconomPrice": 5000,
    "businessPrice": 10000
  }
]
```

---

## 🐛 Решение проблем

### ❌ Backend не запускается

```bash
# Проверьте, запущен ли MongoDB
# Windows: нужно запустить MongoDB сервис
# Linux/Mac: mongod

# Или используйте MongoDB Atlas (облачный)
# Замените MONGODB_URI на: mongodb+srv://user:password@cluster.mongodb.net/vizier_airways
```

### ❌ Frontend не подключается к Backend

```bash
# Проверьте NEXT_PUBLIC_API_URL в frontend/.env.local
# Убедитесь, что Backend запущен на порту 4000
# Очистите кеш браузера (Ctrl+Shift+Delete)
```

### ❌ "Cannot find module" ошибка

```bash
# Удалите node_modules и переустановите
rm -rf node_modules package-lock.yaml
npm install
```

### ❌ Порт 4000 или 9999 занят

```bash
# Для Backend (найти и закрыть процесс на порту 4000)
# Windows:
netstat -ano | findstr :4000
taskkill /PID <PID> /F

# Linux/Mac:
lsof -i :4000
kill -9 <PID>
```

---

## 📋 Основные страницы

| Страница         | URL                      | Описание                    |
| ---------------- | ------------------------ | --------------------------- |
| Главная          | `/`                      | Поиск и список полетов      |
| Вход/Регистрация | `/auth`                  | Аутентификация              |
| Профиль          | `/profile`               | Профиль пользователя        |
| Бронирования     | `/bookings`              | Управление бронированиями   |
| Оплата           | `/payment?bookingId=...` | Форма оплаты                |
| Админ-панель     | `/admin`                 | Управление системой (админ) |
| FAQ              | `/faq`                   | Часто задаваемые вопросы    |

---

## 🎯 API Endpoints для тестирования

### Регистрация

```bash
curl -X POST http://localhost:4000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "test_user",
    "email": "test@example.com",
    "password": "TestPass123",
    "firstName": "Test",
    "lastName": "User",
    "phoneNumber": "+7 999 999 99 99"
  }'
```

### Вход

```bash
curl -X POST http://localhost:4000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "TestPass123"
  }'
```

### Поиск полетов

```bash
curl -X POST http://localhost:4000/api/flights/getFlightByRoute \
  -H "Content-Type: application/json" \
  -d '{
    "from": "Москва",
    "to": "Казань"
  }'
```

---

## 📚 Дополнительная информация

- Полная документация: см. `FULL_PROJECT_README.md`
- Frontend документация: см. `frontend/FRONTEND_README.md`
- Backend README: см. `backend/README.md`

---

## ✨ Готово!

Теперь вы можете использовать полнофункциональное приложение для бронирования авиабилетов! 🎉

**Вопросы?** Проверьте FAQ на странице `/faq`

---

**Версия:** 1.0.0  
**Статус:** ✅ Готово к использованию  
**Дата:** февраль 2025
