# 🎫 Vizier Airways - Система бронирования авиабилетов

Полнофункциональное веб-приложение для бронирования авиабилетов, разработанное на **Next.js** (frontend) и **Node.js/Express** (backend).

## 📑 Содержание

- [Функциональность](#функциональность)
- [Архитектура проекта](#архитектура-проекта)
- [Установка](#установка)
- [Структура фронтенда](#структура-фронтенда)
- [API Эндпоинты](#api-эндпоинты)
- [Использование](#использование)
- [Тестирование](#тестирование)

## ✨ Функциональность

### Для пользователей:

- 👤 **Регистрация и вход** - создание аккаунта и аутентификация
- ✈️ **Поиск полетов** - поиск по маршруту (откуда-куда)
- 🎫 **Бронирование билетов** - выбор класса и добавление пассажиров
- 💳 **Оплата** - безопасная обработка платежей с валидацией карт
- 📋 **Управление бронированиями** - просмотр, отмена бронирований
- 👥 **Профиль пользователя** - редактирование личных данных
- ❓ **FAQ** - часто задаваемые вопросы

### Для администраторов:

- 👥 **Управление пользователями** - просмотр всех пользователей
- ✈️ **Управление рейсами** - добавление одного рейса или массового импорта
- 📊 **Статистика** - просмотр информации о пользователях и рейсах

## 🏗️ Архитектура проекта

```
Vizier Airways/
├── backend/
│   ├── app/
│   │   ├── config/          # Конфигурация (БД, email)
│   │   ├── controllers/     # Обработчики запросов
│   │   ├── middlewares/     # Middleware (auth, validation)
│   │   ├── models/          # MongoDB схемы
│   │   ├── routes/          # API маршруты
│   │   ├── utils/           # Утилиты
│   │   └── validations/     # Схемы валидации
│   ├── app.js               # Точка входа
│   └── package.json
│
└── frontend/
    ├── app/                 # Next.js приложение
    │   ├── page.tsx         # Главная страница
    │   ├── auth/            # Страница входа/регистрации
    │   ├── profile/         # Профиль пользователя
    │   ├── bookings/        # Список бронирований
    │   ├── payment/         # Страница оплаты
    │   ├── admin/           # Админ-панель
    │   └── faq/             # FAQ страница
    ├── features/            # React компоненты
    │   ├── Search/          # Поиск полетов
    │   ├── Header/          # Навигация
    │   └── Booking/         # Форма бронирования
    ├── shared/              # Общие ресурсы
    │   ├── api/             # API хуки
    │   ├── types/           # TypeScript типы
    │   ├── utils/           # Утилиты
    │   └── ui/              # UI компоненты
    └── public/              # Статические файлы
```

## 🚀 Установка

### Требования:

- Node.js >= 18.0.0
- MongoDB >= 5.0.0
- npm или pnpm

### Backend установка:

```bash
cd backend
npm install
# или
pnpm install

# Создайте файл .env
# Шаблон .env:
PORT=4000
MONGODB_URI=mongodb://localhost:27017/vizier_airways
JWT_SECRET=your_jwt_secret_key
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_email_password
FRONTEND_URL=http://localhost:9999

# Запустите backend
npm run dev
```

### Frontend установка:

```bash
cd frontend
npm install
# или
pnpm install

# Создайте файл .env.local
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Запустите frontend
npm run dev
```

Frontend будет доступен на `http://localhost:9999`

## 📂 Структура фронтенда

### Страницы (app/):

| Файл                | Описание         | Функционал                            |
| ------------------- | ---------------- | ------------------------------------- |
| `page.tsx`          | Главная страница | Поиск и список полетов                |
| `auth/page.tsx`     | Вход/регистрация | Форма входа и создания аккаунта       |
| `profile/page.tsx`  | Профиль          | Просмотр и редактирование данных      |
| `bookings/page.tsx` | Бронирования     | Список всех бронирований пользователя |
| `payment/page.tsx`  | Оплата           | Форма оплаты с валидацией карт        |
| `admin/page.tsx`    | Админ-панель     | Управление пользователями и рейсами   |
| `faq/page.tsx`      | FAQ              | Часто задаваемые вопросы              |

### Компоненты (features/):

- **Search/Search.tsx** - Поиск полетов с фильтрацией
- **Header/Header.tsx** - Навигационная панель
- **Booking/Booking.tsx** - Форма для добавления пассажиров

### API интеграция (shared/api/):

```typescript
// Примеры использования:
import { authApi } from "@/shared/api/auth.api";
import { flightApi } from "@/shared/api/flight.api";
import { bookingApi } from "@/shared/api/booking.api";
import { paymentApi } from "@/shared/api/payment.api";
import { userApi } from "@/shared/api/user.api";
import { adminApi } from "@/shared/api/admin.api";

// Вход
await authApi.login({ email: "user@example.com", password: "123456" });

// Поиск полетов
const flights = await flightApi.searchFlights({ from: "Москва", to: "СПб" });

// Бронирование
const booking = await bookingApi.createBooking({
  flightId: "flight_id",
  cabinClass: "economy",
  passengers: [{ firstName: "Иван", lastName: "Иванов", gender: "male" }],
});

// Оплата
await paymentApi.payBooking({
  bookingId: "booking_id",
  cardNumber: "4111111111111111",
  expMonth: 12,
  expYear: 25,
  cvv: "123",
});
```

## 🔌 API Эндпоинты

### Authentication (POST /api/auth)

```
POST   /register          - Регистрация
POST   /login             - Вход
POST   /logout            - Выход
POST   /send-verify-otp   - Отправить OTP для верификации
POST   /verify-account    - Подтвердить аккаунт
POST   /send-reset-otp    - Отправить OTP для сброса пароля
POST   /reset-password    - Сбросить пароль
POST   /is-auth           - Проверить аутентификацию
```

### Flights (POST /api/flights)

```
POST   /getAllRoutes      - Получить все маршруты
POST   /getFlightByRoute  - Поиск полетов по маршруту
```

### Bookings (GET/POST /api/bookings)

```
POST   /                  - Создать бронирование
GET    /me                - Получить мои бронирования
GET    /:id               - Получить бронирование по ID
PATCH  /:id/cancel        - Отменить бронирование
```

### Payments (POST /api/payments)

```
POST   /pay               - Обработить платеж
```

### User (GET/PUT /api/user)

```
GET    /profile           - Получить профиль пользователя
PUT    /update            - Обновить профиль
```

### Admin (GET/POST /api/admin)

```
GET    /users             - Получить всех пользователей
POST   /flight/add        - Добавить один рейс
POST   /flight/add/bulk   - Добавить рейсы массово
```

## 💻 Использование

### Регистрация нового пользователя:

1. Перейдите на `/auth`
2. Выберите вкладку "Регистрация"
3. Заполните форму со всеми данными
4. Нажмите "Зарегистрироваться"

### Поиск и бронирование полета:

1. На главной странице используйте поле поиска
2. Введите город вылета и прибытия
3. Нажмите кнопку поиска
4. Выберите рейс и нажмите "Забронировать"
5. Заполните данные пассажиров
6. Выберите класс салона
7. Нажмите "Подтвердить и оплатить"

### Оплата билета:

1. На странице оплаты введите данные карты
2. Используйте тестовую карту: `4111 1111 1111 1111`
3. CVV: любой 3-значный код
4. Срок действия: любой будущий месяц/год
5. Нажмите "Оплатить"

### Управление профилем:

1. Перейдите на `/profile`
2. Нажмите "Редактировать профиль"
3. Измените нужные данные
4. Нажмите "Сохранить изменения"

### Админ-панель:

1. Авторизуйтесь с админ-аккаунтом
2. На странице профиля нажмите "Админ-панель"
3. Используйте вкладки для:
   - Просмотра всех пользователей
   - Добавления одного рейса
   - Массового импорта рейсов (JSON)

## 🧪 Тестирование

### Тестовые данные для оплаты:

```
Номер карты:  4111 1111 1111 1111
Месяц:        01-12 (любой)
Год:          25-99 (будущее)
CVV:          123 или любой 3-значный код
```

### Пример JSON для массового импорта рейсов:

```json
[
  {
    "from": "Москва",
    "fromAirport": "SVO",
    "to": "Казань",
    "toAirport": "KZN",
    "operatedBy": "Аэрофлот",
    "flightNumber": "SU100",
    "airplaneType": "Boeing 737",
    "departureTime": "2025-02-15T10:00:00",
    "arrivalTime": "2025-02-15T12:00:00",
    "flightDuration": "2h 0min",
    "numberOfTransfers": "0",
    "EconomPrice": 4500,
    "businessPrice": 9000
  }
]
```

## 🔐 Безопасность

- ✅ Валидация данных с использованием Zod
- ✅ JWT аутентификация
- ✅ Защита от CORS атак
- ✅ Валидация номеров кредитных карт (алгоритм Луна)
- ✅ Хеширование паролей
- ✅ Middlewares для проверки прав доступа

## 📝 Лицензия

Этот проект создан для образовательных целей.

## 👨‍💻 Авторы

Разработано как полнофункциональное приложение для бронирования авиабилетов.

---

**Версия:** 1.0.0  
**Последнее обновление:** февраль 2025
